mixin check (name, id, value, id2, value2, mod)
  input(type="radio" id=id name=name class=`radioCheck radioCheck_${mod}`)
  label(for=id)=value
  input(type="radio" id=id2 name=name class=`radioCheck radioCheck_${mod}`)
  label(for=id)=value2